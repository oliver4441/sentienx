services:
  - type: web
    name: sentienx
    runtime: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /
    autoDeploy: true
    envVars:
      - key: DERIV_APP_ID
        value: "113241"
      - key: DERIV_REDIRECT_URI
        value: https://sentienx.onrender.com/api/auth/deriv/callback
      - key: DERIV_MARKUP
        value: "0"
      - key: NODE_ENV
        value: production
      - key: NPM_CONFIG_PRODUCTION
        value: "false"
